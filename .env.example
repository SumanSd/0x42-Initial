# Reserved configuration for storage adapters; these are not loaded by the scaffold.
SKILL_EROSION_SQLITE_PATH=data/processed/traces.sqlite3
SKILL_EROSION_CHROMA_PATH=data/vector_store
# Choose and pin an open-source embedding model during Phase 1.
SKILL_EROSION_EMBEDDING_MODEL=
