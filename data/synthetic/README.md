# Synthetic seed data

All records are invented and use opaque demo IDs. JSON and CSV contain identical
attempts. Expected trend labels are evaluation metadata, not training inputs.
See `docs/data-contract.md` for the four scenarios and their limitations.
