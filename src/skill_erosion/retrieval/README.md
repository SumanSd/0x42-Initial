# Retrieval work area

Add resource loading, resource embedding, and similarity retrieval here.
Keep a separate resource collection from the student attempt collection.
Query with the cluster's underlying misconception, not just `skill_id`.
Retain resource IDs/citations. Use `no_matching_resource` when retrieval has no
adequate match. A language model is optional; curated retrieval is sufficient.
