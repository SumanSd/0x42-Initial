# Integration test backlog

Test import -> durable versions -> embeddings -> four MCP tools -> separate
teacher/student outputs. Include restart persistence, retry after index failure,
no cross-student retrieval, and MCP timeout behavior. Current agents are stubs.
