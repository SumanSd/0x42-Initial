# Unit test backlog

- Trace collector: invalid fields, exact reimport, conflicting version, new version.
- Divergence: matched pairs, missing condition, mismatched rubric/difficulty,
  three ordered checkpoints, widening/narrowing/stable, contradictory signals.
- Clustering: semantically related but differently worded errors; sparse/no weak work.
- Remediation: cluster-specific retrieval, cited resource, no suitable resource.
- Privacy: assert logs contain none of the seeded submission strings.

Write behavioral tests with each implementation; no placeholder passing tests here.
